---
trigger: always_on
---

- when working in 'frontend', 'nginx', 'database'; whenever you finish all the given tasks, instead of building the code, you should rather run - docker compose down; docker compose build; docker compose up -d; in the root of the project directory. 