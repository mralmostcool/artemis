services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: artemis-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    expose:
      - "8080"
